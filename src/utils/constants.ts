export enum ProductType {
  PRODUK = "Produk",
  KOI_STORE = "Koi Store",
}

export const PRODUCT_TYPES = Object.values(ProductType);

// Predefined categories
export enum DefaultCategories {
  PAKAN_IKAN = "Pakan Ikan",
  POND_ATTACHMENT = "Pond Attachment",
}

export const DEFAULT_CATEGORIES = Object.values(DefaultCategories);
